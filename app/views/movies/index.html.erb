<div class="container my-3 pt-1">
  <h1 class="my-3">Movies</h1>
  <div class="row">
    <% @movies.each do |movie| %>
      <div class="col-6 col-md-4 col-lg-3 col-xl-2">
        <%# <%= link_to movie_path(movie) do %>
          <div class="card-movie my-3">
            <%= image_tag movie.poster_url %>
          </div>
        <%# <% end %>
      </div>
    <% end %>
  </div>
</div>
